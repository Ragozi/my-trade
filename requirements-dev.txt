# Development / quality tooling (Phase 0+). Install: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
mypy>=1.11
pydantic>=2.0          # used by config + Claude output validation (Phase 1/4)
